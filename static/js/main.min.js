document.addEventListener('DOMContentLoaded',function(){const loadingOverlay=document.getElementById('loading-overlay');function showLoader(){if(loadingOverlay){loadingOverlay.classList.add('show')}}
function hideLoader(){if(loadingOverlay){loadingOverlay.classList.remove('show')}}
hideLoader();document.querySelectorAll('a').forEach(link=>{link.addEventListener('click',function(e){if(this.target==='_blank'||this.href.startsWith('javascript:')||this.classList.contains('no-loader')){return}
if(this.hash&&(this.pathname===window.location.pathname)){return}
const tgl=this.getAttribute('data-bs-toggle');if(tgl==='dropdown'||tgl==='collapse'||tgl==='modal'){return}
const href=(this.getAttribute('href')||'').toLowerCase();if(href.includes('export')||href.includes('download')||href.endsWith('.docx')||href.endsWith('.csv')||href.endsWith('.xlsx')){return}
showLoader();setTimeout(hideLoader,8000)})});document.querySelectorAll('form').forEach(form=>{form.addEventListener('submit',function(){if(this.target==='_blank'||this.classList.contains('no-loader'))return;showLoader();setTimeout(hideLoader,8000)})});window.addEventListener('load',hideLoader);window.addEventListener('pageshow',function(event){if(event.persisted){hideLoader()}});const toggleButton=document.getElementById('darkModeToggle');const body=document.body;const toggleIcon=toggleButton?toggleButton.querySelector('i'):null;function applyTheme(theme){if(theme==='dark'){body.classList.add('dark-mode');if(toggleIcon){toggleIcon.classList.remove('fa-moon');toggleIcon.classList.add('fa-sun')}
localStorage.setItem('theme','dark')}else{body.classList.remove('dark-mode');if(toggleIcon){toggleIcon.classList.remove('fa-sun');toggleIcon.classList.add('fa-moon')}
localStorage.setItem('theme','light')}
try{const metaLight=document.querySelector('meta[media="(prefers-color-scheme: light)"][name="theme-color"]');const metaDark=document.querySelector('meta[media="(prefers-color-scheme: dark)"][name="theme-color"]');if(metaLight&&metaDark){if(theme==='dark'){metaDark.setAttribute('content',getComputedStyle(document.documentElement).getPropertyValue('--bg-main').trim()||'#1a1a1a')}else{metaLight.setAttribute('content',getComputedStyle(document.documentElement).getPropertyValue('--bg-main').trim()||'#f8f9fa')}}}catch(e){}}
if(toggleButton&&toggleIcon){let currentTheme=localStorage.getItem('theme');if(!currentTheme){currentTheme=window.matchMedia&&window.matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light'}
applyTheme(currentTheme);try{toggleButton.setAttribute('aria-pressed',(currentTheme==='dark').toString());toggleButton.setAttribute('title',currentTheme==='dark'?'Comută la tema luminoasă':'Comută la tema întunecată')}catch(e){}
toggleButton.addEventListener('click',function(){currentTheme=body.classList.contains('dark-mode')?'light':'dark';applyTheme(currentTheme);try{const isPressed=this.getAttribute('aria-pressed')==='true';this.setAttribute('aria-pressed',(!isPressed).toString());this.setAttribute('title',currentTheme==='dark'?'Comută la tema luminoasă':'Comută la tema întunecată')}catch(e){}});try{const media=window.matchMedia('(prefers-color-scheme: dark)');media.addEventListener('change',(e)=>{const explicit=localStorage.getItem('theme');if(!explicit){applyTheme(e.matches?'dark':'light')}})}catch(e){}}else{if(!toggleButton)console.warn("Dark mode toggle button not found.");if(!toggleIcon&&toggleButton)console.warn("Dark mode toggle icon element not found within the button.")}
window.addEventListener('storage',(e)=>{if(e.key==='theme'&&(e.newValue==='dark'||e.newValue==='light')){applyTheme(e.newValue);if(toggleButton){try{toggleButton.setAttribute('aria-pressed',(e.newValue==='dark').toString())}catch(err){}}}});console.log("Main JS loaded. Dark Mode functionality initialized if elements found.");document.querySelectorAll('.copy-btn').forEach(button=>{button.addEventListener('click',function(){const targetSelector=this.dataset.target;const targetElement=document.querySelector(targetSelector);if(targetElement){const textToCopy=targetElement.innerText||targetElement.textContent;navigator.clipboard.writeText(textToCopy).then(()=>{const originalText=this.innerHTML;this.innerHTML='<i class="fas fa-check"></i> Copiat!';this.classList.remove('btn-outline-secondary');this.classList.add('btn-success');setTimeout(()=>{this.innerHTML=originalText;this.classList.remove('btn-success');this.classList.add('btn-outline-secondary')},2000)}).catch(err=>{console.error('Eroare la copierea textului: ',err);alert('Eroare la copierea textului. Este posibil ca browserul dvs. să nu suporte această funcționalitate.')})}})});function hasTableOnPage(){return document.querySelector('.table')!==null}
function createFloatingActions(){if(!hasTableOnPage())return;const container=document.createElement('div');container.className='fab-container';const printBtn=document.createElement('button');printBtn.className='btn btn-secondary fab-button no-loader';printBtn.type='button';printBtn.title='Printează pagina (Shift+P)';printBtn.innerHTML='<i class="fas fa-print"></i>';printBtn.addEventListener('click',()=>window.print());const topBtn=document.createElement('button');topBtn.className='btn btn-secondary fab-button no-loader';topBtn.type='button';topBtn.title='Mergi sus (Shift+T)';topBtn.innerHTML='<i class="fas fa-arrow-up"></i>';topBtn.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));container.appendChild(printBtn);container.appendChild(topBtn);document.body.appendChild(container)}
createFloatingActions();document.addEventListener('keydown',(e)=>{if(e.shiftKey&&!e.ctrlKey&&!e.altKey){if(e.key.toLowerCase()==='d'){if(toggleButton)toggleButton.click()}else if(e.key.toLowerCase()==='p'){window.print()}else if(e.key.toLowerCase()==='t'){window.scrollTo({top:0,behavior:'smooth'})}}});function restoreFormState(form){try{const key='form:'+location.pathname;const saved=localStorage.getItem(key);if(!saved)return;const data=JSON.parse(saved);Array.from(form.elements).forEach(el=>{if(!el.name)return;if(el.type==='checkbox'||el.type==='radio'){el.checked=!!data[el.name]}else if(el.tagName==='SELECT'){if(Array.isArray(data[el.name])){Array.from(el.options).forEach(opt=>opt.selected=data[el.name].includes(opt.value))}else{el.value=data[el.name]??el.value}}else{el.value=data[el.name]??el.value}})}catch(e){}}
function saveFormState(form){try{const key='form:'+location.pathname;const data={};Array.from(form.elements).forEach(el=>{if(!el.name)return;if(el.type==='checkbox'){data[el.name]=el.checked}else if(el.type==='radio'){if(el.checked)data[el.name]=el.value}else if(el.tagName==='SELECT'&&el.multiple){data[el.name]=Array.from(el.selectedOptions).map(o=>o.value)}else{data[el.name]=el.value}});localStorage.setItem(key,JSON.stringify(data))}catch(e){}}
document.querySelectorAll('form').forEach(form=>{const isGet=(form.method||'GET').toUpperCase()==='GET';const isAuth=/login|logout|password/i.test(form.action||'')||form.querySelector('input[type="password"]');if(!isGet||isAuth)return;restoreFormState(form);form.addEventListener('change',()=>saveFormState(form));form.addEventListener('input',()=>saveFormState(form))});function inferType(value){const v=value.trim();if(!v)return'text';const num=v.replace(/\s/g,'').replace(',','.');if(!isNaN(num)&&num!=='')return'number';if(/^\d{1,2}[:\.\-]\d{1,2}([:\.\-]\d{2,4})?$/.test(v)||/\d{4}-\d{2}-\d{2}/.test(v))return'text-dateish';return'text'}
function getCellText(cell){return(cell.getAttribute('data-value')||cell.innerText||'').replace(/\s+/g,' ').trim()}
function compare(a,b,type){if(type==='number')return parseFloat(a.replace(',','.'))-parseFloat(b.replace(',','.'));return a.localeCompare(b,undefined,{sensitivity:'base',numeric:true})}
function makeTableSortable(table){const thead=table.querySelector('thead');if(!thead)return;thead.querySelectorAll('th').forEach((th,index)=>{if(th.classList.contains('no-sort'))return;th.classList.add('th-sortable');th.tabIndex=0;th.setAttribute('role','button');th.addEventListener('click',()=>sortBy(index,th));th.addEventListener('keydown',(e)=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();sortBy(index,th)}})});function sortBy(colIndex,th){const tbody=table.querySelector('tbody');if(!tbody)return;const rows=Array.from(tbody.querySelectorAll('tr'));if(rows.length===0)return;const sample=getCellText(rows[0].children[colIndex]||{innerText:''});const type=inferType(sample);const current=th.getAttribute('aria-sort');const direction=current==='ascending'?'descending':'ascending';thead.querySelectorAll('th[aria-sort]').forEach(x=>x.removeAttribute('aria-sort'));th.setAttribute('aria-sort',direction);rows.sort((r1,r2)=>{const a=getCellText(r1.children[colIndex]||{innerText:''});const b=getCellText(r2.children[colIndex]||{innerText:''});const cmp=compare(a,b,type);return direction==='ascending'?cmp:-cmp});rows.forEach(r=>tbody.appendChild(r))}}
document.querySelectorAll('table.table').forEach(makeTableSortable);function enhanceTable(table){const thead=table.querySelector('thead');const tbody=table.querySelector('tbody');if(!thead||!tbody)return;const tools=document.createElement('div');tools.className='table-tools d-flex flex-wrap align-items-center gap-2 mb-2';const filterInput=document.createElement('input');filterInput.className='form-control form-control-sm table-filter-input';filterInput.type='search';filterInput.placeholder='Filtrează rânduri (Shift+F)';filterInput.autocomplete='off';tools.appendChild(filterInput);const densityBtn=document.createElement('button');densityBtn.className='btn btn-outline-secondary btn-sm';densityBtn.type='button';densityBtn.title='Comută densitatea tabelului';densityBtn.innerHTML='<i class="fas fa-compress-arrows-alt"></i>';densityBtn.addEventListener('click',()=>{table.classList.toggle('table-compact')});tools.appendChild(densityBtn);const dropdown=document.createElement('div');dropdown.className='dropdown d-inline-block';dropdown.innerHTML='<button class="btn btn-outline-secondary btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown">Coloane</button><div class="dropdown-menu p-2 columns-menu" style="min-width: 220px;"></div>';const menu=dropdown.querySelector('.columns-menu');tools.appendChild(dropdown);const wrap=table.closest('.table-responsive');if(wrap&&wrap.parentElement)wrap.parentElement.insertBefore(tools,wrap);else table.parentElement.insertBefore(tools,table);const ths=Array.from(thead.querySelectorAll('th'));const storageKey='columns:'+location.pathname;let visible=ths.map(()=>true);try{const saved=JSON.parse(localStorage.getItem(storageKey)||'null');if(Array.isArray(saved)&&saved.length===visible.length)visible=saved}catch(e){}
function applyColumnVisibility(){Array.from(table.querySelectorAll('tr')).forEach(tr=>{ths.forEach((_,idx)=>{const cell=tr.children[idx];if(cell)cell.style.display=(visible[idx]?'':'none')})})}
function saveVisibility(){try{localStorage.setItem(storageKey,JSON.stringify(visible))}catch(e){}}
ths.forEach((th,idx)=>{const label=document.createElement('label');label.className='dropdown-item d-flex align-items-center gap-2';const cb=document.createElement('input');cb.type='checkbox';cb.className='form-check-input';cb.checked=visible[idx];cb.addEventListener('change',()=>{visible[idx]=cb.checked;applyColumnVisibility();saveVisibility()});label.appendChild(cb);const span=document.createElement('span');span.textContent=(th.innerText||th.textContent||`Col ${idx+1}`).trim()||`Col ${idx+1}`;label.appendChild(span);menu.appendChild(label)});applyColumnVisibility();function filterRows(){const q=filterInput.value.trim().toLowerCase();const trs=Array.from(tbody.querySelectorAll('tr'));if(!q){trs.forEach(tr=>tr.style.display='');return}
trs.forEach(tr=>{const text=(tr.innerText||'').toLowerCase();tr.style.display=text.includes(q)?'':'none'})}
filterInput.addEventListener('input',filterRows);document.addEventListener('keydown',(e)=>{if(e.shiftKey&&!e.ctrlKey&&!e.altKey&&e.key.toLowerCase()==='f'){filterInput.focus();e.preventDefault()}});tbody.addEventListener('click',(e)=>{const target=e.target;if(target.closest('a, button, input, label, select, textarea'))return;const tr=target.closest('tr');if(!tr)return;tr.classList.toggle('row-selected')})}
document.querySelectorAll('table.table').forEach(enhanceTable);function exportTableToCSV(){const table=document.querySelector('table.table');if(!table){alert('Tabelul nu a fost găsit pentru export.');return}
const selectedRows=table.querySelectorAll('tbody tr.row-selected');if(selectedRows.length===0){alert('Nu ați selectat niciun rând pentru export.');return}
const headersToExport=[];const indicesToExport=[];table.querySelectorAll('thead th').forEach((th,index)=>{if(th.style.display!=='none'&&th.innerText.trim().toLowerCase()!=='acțiuni'){const headerText=`"${th.innerText.trim().replace(/"/g,'""')}"`;headersToExport.push(headerText);indicesToExport.push(index)}});const csvRows=[headersToExport.join(',')];selectedRows.forEach(row=>{const rowData=[];const cells=Array.from(row.cells);indicesToExport.forEach(index=>{if(cells[index]){const cellText=`"${cells[index].innerText.trim().replace(/"/g,'""')}"`;rowData.push(cellText)}else{rowData.push('""')}});csvRows.push(rowData.join(','))});const bom=new Uint8Array([239,187,191]);const csvString=csvRows.join('\\n');const blob=new Blob([bom,csvString],{type:'text/csv;charset=utf-8;'});const link=document.createElement("a");if(link.download!==undefined){const url=URL.createObjectURL(blob);const today=new Date();const dateStr=today.toISOString().split('T')[0];link.setAttribute("href",url);link.setAttribute("download",`export_selectie_${dateStr}.csv`);link.style.visibility='hidden';document.body.appendChild(link);link.click();document.body.removeChild(link);URL.revokeObjectURL(url)}else{alert("Browser-ul dumneavoastră nu suportă descărcarea fișierelor. Vă rugăm încercați un browser modern.")}}
(function initSelectionBar(){const table=document.querySelector('table.table');if(!table)return;const bar=document.createElement('div');bar.className='selection-bar shadow-sm';bar.innerHTML='<div class="container d-flex align-items-center justify-content-between gap-2 py-2">'+'<div class="text-muted small sel-info">0 rânduri selectate</div>'+'<div class="d-flex gap-2">'+'  <button type="button" class="btn btn-sm btn-outline-secondary sel-select-all">Selectează tot</button>'+'  <button type="button" class="btn btn-sm btn-outline-secondary sel-clear">Golește selecția</button>'+'  <button type="button" class="btn btn-sm btn-primary sel-export">Exportă selecția CSV</button>'+'  <button type="button" class="btn btn-sm btn-secondary sel-copy">Copiază selecția</button>'+'</div></div>';document.body.appendChild(bar);const info=bar.querySelector('.sel-info');function update(){const count=table.querySelectorAll('tbody tr.row-selected').length;info.textContent=count+(count===1?' rând selectat':' rânduri selectate');const isShown=count>0;bar.classList.toggle('show',isShown);document.body.classList.toggle('selection-bar-shown',isShown)}
table.addEventListener('click',(e)=>{if(e.target.closest('tbody'))setTimeout(update,0)});bar.querySelector('.sel-select-all').addEventListener('click',()=>{table.querySelectorAll('tbody tr').forEach(tr=>tr.classList.add('row-selected'));update()});bar.querySelector('.sel-clear').addEventListener('click',()=>{table.querySelectorAll('tbody tr.row-selected').forEach(tr=>tr.classList.remove('row-selected'));update()});bar.querySelector('.sel-export').addEventListener('click',exportTableToCSV);bar.querySelector('.sel-copy').addEventListener('click',()=>{const tableEl=document.querySelector('.table');if(!tableEl)return;const rows=Array.from(tableEl.querySelectorAll('tbody tr.row-selected'));if(!rows.length)return;const text=rows.map(r=>Array.from(r.querySelectorAll('td')).map(td=>td.innerText.trim()).join('\t')).join('\n');navigator.clipboard.writeText(text).catch(()=>{})});update()})();(function swRegister(){if('serviceWorker'in navigator){navigator.serviceWorker.register('/static/sw.js').catch(()=>{})}})();(function initCommandPalette(){const allLinks=Array.from(document.querySelectorAll('a[href]')).filter(a=>a.href&&!a.href.startsWith('javascript:')&&!a.getAttribute('href').startsWith('#')).map(a=>({href:a.getAttribute('href'),text:(a.innerText||a.title||a.href).replace(/\s+/g,' ').trim()})).filter(x=>x.text).reduce((acc,cur)=>{if(!acc.find(y=>y.href===cur.href))acc.push(cur);return acc},[]);const modal=document.createElement('div');modal.className='modal';modal.tabIndex=-1;modal.innerHTML='<div class="modal-dialog modal-dialog-centered"><div class="modal-content command-palette">'+'<div class="modal-header py-2"><input type="search" class="form-control form-control-lg cp-input" placeholder="Caută... (Ctrl+K)"/></div>'+'<div class="modal-body p-0"><ul class="list-group list-group-flush cp-list" style="max-height:50vh;overflow:auto"></ul></div>'+'</div></div>';document.body.appendChild(modal);let bsModal=null;try{bsModal=new bootstrap.Modal(modal,{backdrop:true})}catch(_){}
const input=modal.querySelector('.cp-input');const list=modal.querySelector('.cp-list');function render(items){list.innerHTML='';items.slice(0,50).forEach(it=>{const li=document.createElement('li');li.className='list-group-item list-group-item-action';li.textContent=it.text;li.addEventListener('click',()=>{window.location.href=it.href});list.appendChild(li)})}
function openPalette(){render(allLinks);if(bsModal)bsModal.show();else modal.style.display='block';setTimeout(()=>input.focus(),100)}
function closePalette(){if(bsModal)bsModal.hide();else modal.style.display='none'}
input.addEventListener('input',()=>{const q=input.value.toLowerCase().trim();if(!q)return render(allLinks);const filtered=allLinks.filter(x=>x.text.toLowerCase().includes(q));render(filtered)});input.addEventListener('keydown',(e)=>{if(e.key==='Escape'){closePalette()}});document.addEventListener('keydown',(e)=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();openPalette()}})})();(function formHelpers(){const candidates=Array.from(document.querySelectorAll('input.has-time-helpers'));if(!candidates.length)return;function getBucharestDateTimeParts(){const now=new Date();const options={timeZone:'Europe/Bucharest',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hourCycle:'h23'};const formatter=new Intl.DateTimeFormat('sv-SE',options);const formattedString=formatter.format(now);const[datePart,timePart]=formattedString.split(' ');return{date:datePart,time:timePart}}
candidates.forEach(input=>{const wrap=document.createElement('div');wrap.className='input-group mb-2';const parent=input.parentNode;parent.insertBefore(wrap,input);wrap.appendChild(input);function applyTime(){const parts=getBucharestDateTimeParts();if(input.type==='time'){input.value=parts.time}else if(input.type==='date'){input.value=parts.date}else if(input.type==='datetime-local'){input.value=`${parts.date}T${parts.time}`}
input.dispatchEvent(new Event('change',{bubbles:true}))}
const btnNow=document.createElement('button');btnNow.type='button';btnNow.className='btn btn-outline-secondary';btnNow.textContent='Acum';btnNow.title='Setează ora curentă';btnNow.addEventListener('click',applyTime);const btnGroup=document.createElement('div');btnGroup.append(btnNow);wrap.appendChild(btnGroup)})})();(function netStatus(){function toast(msg,variant){const el=document.createElement('div');el.className=`toast align-items-center text-bg-${variant||'secondary'} border-0 show`;el.setAttribute('role','status');el.setAttribute('aria-live','polite');el.setAttribute('aria-atomic','true');el.style.position='fixed';el.style.right='12px';el.style.bottom='12px';el.style.zIndex='1060';el.innerHTML=`<div class="d-flex"><div class="toast-body">${msg}</div><button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button></div>`;document.body.appendChild(el);setTimeout(()=>{try{el.remove()}catch(_){}},3000)}
window.addEventListener('online',()=>toast('Conexiune restabilită','success'));window.addEventListener('offline',()=>toast('Ești offline','warning'))})();(function favorites(){const KEY='favorites:links';const current={href:location.pathname+location.search,title:document.title.replace(/\s*[-|].*$/,'').trim()||location.pathname};function get(){try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch(_){return[]}}
function set(v){try{localStorage.setItem(KEY,JSON.stringify(v))}catch(_){}}
function isFav(favs){return!!favs.find(x=>x.href===current.href)}
const favToggle=document.createElement('button');favToggle.className='btn btn-outline-secondary btn-sm no-loader ms-2';favToggle.title='Adaugă/șterge din Favorite';favToggle.innerHTML='<i class="far fa-star"></i>';const nav=document.querySelector('#navbarNav .navbar-nav');if(nav){const li=document.createElement('li');li.className='nav-item d-flex align-items-center';li.appendChild(favToggle);nav.appendChild(li)}
function refreshIcon(){const favs=get();favToggle.innerHTML=isFav(favs)?'<i class="fas fa-star"></i>':'<i class="far fa-star"></i>'}
favToggle.addEventListener('click',()=>{const favs=get();const i=favs.findIndex(x=>x.href===current.href);if(i>=0)favs.splice(i,1);else favs.unshift(current);set(favs.slice(0,15));refreshIcon();renderMenu()});refreshIcon();function renderMenu(){const dd=document.querySelector('#favoritesDropdown');if(!dd)return;const ul=dd.querySelector('.dropdown-menu');const favs=get();dd.style.display=favs.length?'':'none';ul.innerHTML='<li class="dropdown-header">Favorite</li><li><hr class="dropdown-divider"></li>'+(favs.map(x=>`<li><a class="dropdown-item" href="${x.href}"><i class=\"fas fa-star text-warning\"></i> ${x.title}</a></li>`).join('')||'<li class="px-3 text-muted small">Nicio pagină favorită încă</li>')}})();(function savedViews(){const forms=Array.from(document.querySelectorAll('form')).filter(f=>(f.method||'GET').toUpperCase()==='GET'&&!/login|logout|password/i.test(f.action||''));if(!forms.length)return;const container=document.createElement('div');container.className='d-inline-block ms-2';container.innerHTML='<div class="dropdown d-inline-block">            <button class="btn btn-outline-secondary btn-sm dropdown-toggle no-loader" type="button" data-bs-toggle="dropdown">Vederi</button>            <div class="dropdown-menu p-2" style="min-width:260px">            <div class="d-flex gap-2 mb-2">              <input type="text" class="form-control form-control-sm sv-name" placeholder="Nume vedere">              <button class="btn btn-sm btn-primary sv-save">Salvează</button>            </div>            <div class="sv-list small"></div>            </div>        </div>';const toolInsertTarget=document.querySelector('.table-responsive')?.parentElement||document.querySelector('.container');if(toolInsertTarget)toolInsertTarget.insertBefore(container,toolInsertTarget.firstChild);const key='views:'+location.pathname;function get(){try{return JSON.parse(localStorage.getItem(key)||'[]')}catch(_){return[]}}
function set(v){try{localStorage.setItem(key,JSON.stringify(v))}catch(_){}}
function capture(){const data={};forms.forEach(f=>Array.from(f.elements).forEach(el=>{if(!el.name)return;if(el.type==='checkbox')data[el.name]=el.checked;else if(el.type==='radio'){if(el.checked)data[el.name]=el.value}else if(el.tagName==='SELECT'&&el.multiple){data[el.name]=Array.from(el.selectedOptions).map(o=>o.value)}else{data[el.name]=el.value}}));return data}
function apply(data){forms.forEach(f=>Array.from(f.elements).forEach(el=>{if(!el.name)return;if(el.type==='checkbox')el.checked=!!data[el.name];else if(el.type==='radio'){el.checked=data[el.name]===el.value}else if(el.tagName==='SELECT'&&el.multiple){Array.from(el.options).forEach(o=>o.selected=(data[el.name]||[]).includes(o.value))}else{if(data[el.name]!==undefined)el.value=data[el.name]}}))}
function render(){const list=container.querySelector('.sv-list');const views=get();list.innerHTML='';if(!views.length){list.textContent='Nu există vederi salvate.';return}
views.forEach((v,i)=>{const row=document.createElement('div');row.className='d-flex justify-content-between align-items-center py-1';row.innerHTML=`<span>${v.name}</span><span><button class=\"btn btn-sm btn-outline-secondary me-1\">Aplică</button><button class=\"btn btn-sm btn-outline-danger\">Șterge</button></span>`;const[btnApply,btnDel]=row.querySelectorAll('button');btnApply.addEventListener('click',()=>{apply(v.data);forms[0].submit()});btnDel.addEventListener('click',()=>{const vs=get();vs.splice(i,1);set(vs);render()});list.appendChild(row)})}
container.querySelector('.sv-save').addEventListener('click',()=>{const name=container.querySelector('.sv-name').value.trim()||'Vedere';const vs=get();vs.unshift({name,data:capture()});set(vs.slice(0,20));render()});render()})();(function localReminders(){if(!('Notification'in window))return;const KEY='reminders:enabled';const debugBar=document.querySelector('.container-fluid .text-muted.small');if(debugBar){const btn=document.createElement('button');btn.className='btn btn-sm btn-outline-secondary no-loader';btn.textContent=localStorage.getItem(KEY)==='1'?'Remindere: ON':'Remindere: OFF';btn.addEventListener('click',async()=>{if(localStorage.getItem(KEY)==='1'){localStorage.removeItem(KEY);btn.textContent='Remindere: OFF';return}
const perm=await Notification.requestPermission();if(perm==='granted'){localStorage.setItem(KEY,'1');btn.textContent='Remindere: ON'}});debugBar.parentElement.appendChild(btn)}
if(localStorage.getItem(KEY)!=='1')return;const now=Date.now();document.querySelectorAll('[data-reminder="true"]').forEach(el=>{const start=el.getAttribute('data-start');const title=el.getAttribute('data-title')||document.title;if(!start)return;const ts=new Date(start).getTime();const delta=ts-now-5*60*1000;if(delta>0&&delta<24*60*60*1000){setTimeout(()=>{try{new Notification('Reminder',{body:title})}catch(_){}},delta)}})})();});
